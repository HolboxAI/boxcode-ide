diff --git a/src/vs/workbench/contrib/welcomeAgentSessions/browser/agentSessionsWelcome.ts b/src/vs/workbench/contrib/welcomeAgentSessions/browser/agentSessionsWelcome.ts
index 8e94d53..bab2614 100644
--- a/src/vs/workbench/contrib/welcomeAgentSessions/browser/agentSessionsWelcome.ts
+++ b/src/vs/workbench/contrib/welcomeAgentSessions/browser/agentSessionsWelcome.ts
@@ -5,6 +5,7 @@
 
 import './media/agentSessionsWelcome.css';
 import { $, addDisposableListener, append, clearNode, Dimension, getWindow, scheduleAtNextAnimationFrame } from '../../../../base/browser/dom.js';
+import { mainWindow } from '../../../../base/browser/window.js';
 import { renderIcon } from '../../../../base/browser/ui/iconLabel/iconLabels.js';
 import { DomScrollableElement } from '../../../../base/browser/ui/scrollbar/scrollableElement.js';
 import { Toggle } from '../../../../base/browser/ui/toggle/toggle.js';
@@ -33,7 +34,7 @@ import { IEditorOpenContext, IEditorSerializer } from '../../../common/editor.js
 import { SIDE_BAR_FOREGROUND } from '../../../common/theme.js';
 import { IEditorGroup } from '../../../services/editor/common/editorGroupsService.js';
 import { IEditorService } from '../../../services/editor/common/editorService.js';
-import { IWorkbenchLayoutService } from '../../../services/layout/browser/layoutService.js';
+import { IWorkbenchLayoutService, Parts } from '../../../services/layout/browser/layoutService.js';
 import { ChatAgentLocation, ChatConfiguration, ChatModeKind } from '../../chat/common/constants.js';
 import { ChatContextKeys } from '../../chat/common/actions/chatContextKeys.js';
 import { ChatWidget } from '../../chat/browser/widget/chatWidget.js';
@@ -968,6 +969,24 @@ export class AgentSessionsWelcomePage extends EditorPane {
 		this.chatWidget?.focusInput();
 	}
 
+	/**
+	 * boxcode-ide: the landing page is meant to read as a clean, dedicated
+	 * first-run screen, not a full IDE with an empty sidebar/toolbar already
+	 * showing -- the activity bar (file/search/git/debug/extensions icons)
+	 * and the title bar's right-side icon cluster (editor-layout-split
+	 * controls, the Integrated Browser toggle) are both meaningless before
+	 * any project or session actually exists. Hidden only while this page is
+	 * the visible editor, restored the moment it isn't (a real session
+	 * opening, or "Open Folder") -- this is a visibility toggle on existing
+	 * chrome, not a removal, so normal IDE use past this screen is
+	 * unaffected.
+	 */
+	override setEditorVisible(visible: boolean): void {
+		super.setEditorVisible(visible);
+		this.layoutService.setPartHidden(visible, Parts.ACTIVITYBAR_PART);
+		this.layoutService.getContainer(mainWindow).classList.toggle('boxcode-chrome-minimal', visible);
+	}
+
 	private async revealMaximizedChat(): Promise<void> {
 		try {
 			await this.closeEditorAndMaximizeAuxiliaryBar();
diff --git a/src/vs/workbench/contrib/welcomeAgentSessions/browser/media/agentSessionsWelcome.css b/src/vs/workbench/contrib/welcomeAgentSessions/browser/media/agentSessionsWelcome.css
index 1ce3328..bdd7019 100644
--- a/src/vs/workbench/contrib/welcomeAgentSessions/browser/media/agentSessionsWelcome.css
+++ b/src/vs/workbench/contrib/welcomeAgentSessions/browser/media/agentSessionsWelcome.css
@@ -581,3 +581,20 @@
 		opacity: 0.6;
 	}
 }
+
+/*
+ * boxcode-ide: hides the title bar's right-side icon cluster (editor
+ * layout/split controls, the Integrated Browser toggle) while this welcome
+ * page is the visible editor -- toggled from agentSessionsWelcome.ts's own
+ * setEditorVisible() override, the same lifecycle hook that hides the
+ * activity bar for the same reason. .action-toolbar-container is the real
+ * element (titlebarPart.ts) that both MenuId.TitleBar (the browser toggle
+ * registers here, see browserTabManagementFeatures.ts) and
+ * MenuId.LayoutControlMenu render into -- one rule covers both icon groups
+ * without touching .center-adjacent-toolbar-container (the update
+ * indicator) or .window-controls-container (the actual window
+ * minimize/maximize/close buttons), neither of which this is about.
+ */
+.monaco-workbench.boxcode-chrome-minimal .titlebar-right .action-toolbar-container {
+	display: none;
+}
